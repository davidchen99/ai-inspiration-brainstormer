# AI灵感脑暴器产品重构日志

日期：2026-05-13

> 历史日志：本文记录 2026-05-13 的产品重构阶段。当前生产版本已升级为管理员后台统一配置 API Key，普通用户不再看到“接入 AI / 高级设置”入口。

## 核心摘要

2026-05-13 围绕“微信公众号标题制作”项目完成了一次产品定位重构：产品从原来的“公众号标题与简介发散工具”，明确升级为“AI 灵感脑暴器”。核心目标是帮助内容运营者把一个名称、一句话或几个关键词快速发散成多个内容方向，再选择有潜力的点子继续深入成公众号、小红书或朋友圈内容方案。

本次重点不是单纯加功能，而是从产品体验上做加减法：首屏减负、复杂能力折叠、结果改为瀑布流点子墙、单点详情面板承载深入操作，同时保留批量导出能力。当前已完成页面重构、DeepSeek 接入保留、朋友圈文案新增、Excel/Word/ZIP 导出流程更新，并同步了 README。

## 详细日志

### 2026-05-13 目标

把现有本地 HTML 工具从“标题生成器”调整为更适合小白使用的“AI 灵感脑暴器”。

核心产品目标包括：

- 让用户输入一句想法后，快速得到一批可写方向。
- 让小白第一眼知道当前任务位置，不被 API、飞书、模型、分类库等配置干扰。
- 让高手仍然可以在高级设置里调整模型、接口、风格、分类、目标字数等参数。
- 支持从单个点子继续深入，生成 150 字介绍、公众号方案、小红书方案和朋友圈文案。
- 支持已选点子的复制、Excel 总览、Word 批量导出和 ZIP 打包。

### 2026-05-13 完成

#### 1. 产品定位确认

最终产品名称确定为：

**AI 灵感脑暴器**

产品不再定位为“公众号标题与简介发散”，而是定位为内容创作者的横向发散工具。核心价值是帮助用户从一个点发散到多个面，再从有潜力的面继续深入。

确认后的主流程为：

1. 输入一句想法、名称或几个关键词。
2. 默认生成 10 个脑暴点子。
3. 在点子墙里浏览。
4. 点击某个点子进入详情。
5. 对单个点子继续发散成 150 字以内介绍。
6. 进一步生成公众号、小红书或朋友圈内容方案。
7. 勾选多个点子做批量导出。

#### 2. 首屏做减法

首屏保留：

- 想法输入框
- 3 个案例
- 生成数量
- 开始脑暴
- 生成提示词
- 接入 AI / 高级设置折叠入口（历史版本；当前版本已迁入管理员后台）

移出首屏或折叠：

- 公众号目标字数
- DeepSeek API Key
- API 地址
- 模型选择
- 发散程度
- 风格偏好
- 不想要的内容
- 分类库

这样小白打开页面后，不会先被模型、API 和平台方案干扰，而是直接进入“输入想法 → 开始脑暴”的主任务。

#### 3. 高级设置改为“接入 AI”

高级设置区域改名为：

**接入 AI / 高级设置**（历史版本入口，当前版本已迁入管理员后台）

内部保留 DeepSeek 真实接入能力，并增加 DeepSeek 官网获取 API Key 的跳转链接：

`https://platform.deepseek.com/api_keys`

同时保留 Kimi 和自定义模型入口作为后续预留，但当前版本只真实接入 DeepSeek。

#### 4. 阶段栏改为可点击导航

顶部阶段栏调整为可点击跳转：

- 输入想法
- 点子墙
- 深入发散
- 内容方案
- 导出

并增加状态控制：

- 没有生成点子前，后续阶段不可用。
- 有点子后，可以跳到点子墙和导出区。
- 点开具体点子后，可以跳到深入发散和内容方案。

#### 5. 点子结果改为瀑布流

旧版本结果是列表卡片，信息偏长，容易变成表单式工作台。新版本改为 Pinterest 风格点子墙：

- 桌面端默认 3 列，宽屏 4 列。
- 移动端 1 列。
- 卡片保持短内容，方便一屏多看几个想法。

每张卡片展示：

- 选题方向，作为主标题。
- 一句话简介。
- 推荐标题。
- 平台标签。
- 分类标签。
- 右上角弱化星级。

#### 6. 点击卡片与勾选分离

交互确认采用：

- 点击卡片主体：打开右侧详情面板。
- 勾选框：加入批量导出。

这样“查看和深入一个点子”与“批量选择多个点子”不会混在一起。

视觉状态也做了区分：

- 当前打开详情的卡片高亮。
- 已勾选卡片显示“已选择”，标题加粗。
- 有勾选状态后，未选卡片适度灰暗，方便辨别。

#### 7. 右侧详情面板承载深入操作

详情面板用于展示单个点子的完整信息和下一步操作，避免点子墙不断变长。

详情面板包含：

- 选题方向
- 推荐标题
- 一句话简介
- 目标读者
- 切入角度
- 推荐理由

操作按钮包括：

- 继续发散
- 转公众号
- 转小红书
- 转朋友圈
- 复制当前

生成出的 150 字介绍、公众号方案、小红书方案、朋友圈文案都在详情面板内展示。

#### 8. 新增朋友圈文案能力

新增“转朋友圈”功能。

朋友圈文案定位为：

- 手写感
- 两三句话
- 要么有趣，要么利他
- 让人愿意读完
- 配图一般 3 张

朋友圈方案字段包括：

- 朋友圈文案
- 文案风格
- 3 张配图建议
- 发布场景
- 互动引导

#### 9. 批量操作折叠

批量能力保留，但不再作为小白主路径。

折叠区标题为：

**批量导出与高级操作**

包含：

- 批量继续发散
- 批量转公众号
- 批量转小红书
- 批量转朋友圈
- 复制已选
- 导出 Excel
- 批量导出 Word

主路径变成“点一个，深入一个”；批量能力留给高手或后期集中处理。

#### 10. 导出能力更新

导出继续使用前端 CDN 库：

- `docx` 生成 `.docx`
- `xlsx` 生成 `.xlsx`
- `JSZip` 生成 `.zip`

Word ZIP 结构增加朋友圈文案文件夹：

```text
AI灵感脑暴器-导出-日期.zip
├─ AI灵感脑暴器-总览-日期.xlsx
├─ 脑暴点子/
├─ 公众号方案/
├─ 小红书方案/
└─ 朋友圈文案/
```

Excel 总览新增朋友圈相关字段：

- 朋友圈文案
- 朋友圈配图建议

### 2026-05-13 遇到的问题

#### 1. 旧产品信息架构偏重

旧版本左侧集中放了 API Key、模型、分类库、飞书配置、生成按钮等，对小白来说首屏负担较重。解决方式是把高级能力折叠，并把首屏压缩成最核心的输入和生成。

#### 2. 旧交互偏批量处理，不像灵感探索

旧逻辑是先勾选多个点子，再统一点“继续发散”或“转公众号/小红书”。这个更像批量加工，不符合看到一个好点子后立刻深入的直觉。新版本改为点击卡片进入详情，单点直接深入。

#### 3. 阶段栏原本只是状态展示

旧版本阶段栏不能点击，只是告诉用户当前处在哪一步。新版本改成可点击导航，并做了解锁逻辑。

#### 4. 朋友圈文案需要单独定义

朋友圈不是公众号的短版，也不是小红书的无图版。它更像真人手写的两三句话，需要有趣、利他或生活感，同时配三张图。因此单独设计了朋友圈 prompt 和导出字段。

### 核心结论

这个产品最重要的不是“生成标题”，而是“帮内容创作者横向发散想法”。因此体验上应该优先服务浏览、筛选和单点深入，而不是一开始就让用户配置平台、模型、字数和导出方式。

当前最佳产品路径是：

**输入想法 → 点子墙 → 点击详情 → 单点深入 → 勾选沉淀 → 批量导出**

这条路径既保留了小白可用的极简体验，也给高手留下了高级设置和批量操作。

### 建议工作流

建议使用方式：

1. 先输入一个模糊想法，不要一开始就过度限制风格。
2. 生成 10 个点子，快速扫点子墙。
3. 点击有感觉的点子，在右侧详情里查看完整信息。
4. 对有潜力的点子先点“继续发散”，生成 150 字介绍。
5. 再根据内容形态选择：
   - 长文本知识科普：转公众号。
   - 多图短文案：转小红书。
   - 手写感两三句话：转朋友圈。
6. 最后勾选需要沉淀的点子，批量导出 Excel 或 Word ZIP。

### 后续计划

建议后续继续优化：

- 增加本地保存历史脑暴记录。
- 支持把导出的点子直接同步到飞书 Base。
- 增加模型服务商扩展，比如 Kimi、自定义 OpenAI 兼容接口。
- 增加点子搜索、分类筛选和只看已选。
- 增加一键生成完整公众号正文。
- 增加小红书图片提示词导出，方便后续生成图片。
- 增加朋友圈多风格变体，例如有趣版、利他版、生活感版。
